# Dockerfile alternativo - React rodando diretamente na porta 500
FROM node:20-alpine

# Define o diretório de trabalho
WORKDIR /app

# Copia dependências e instala
COPY package.json ./
COPY package-lock.json ./
RUN npm install

# Copia o código fonte do React
COPY . .

# Define as variáveis de ambiente
ENV PORT=500
ENV REACT_APP_API_URL=http://localhost:2100

# Expõe a porta 500
EXPOSE 500

# Roda o React em modo desenvolvimento na porta 500
CMD ["npm", "start"]
